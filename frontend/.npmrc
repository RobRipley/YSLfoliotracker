# Ensure devDependencies are always installed regardless of NODE_ENV
# This fixes the issue where NODE_ENV=production causes npm to skip dev deps
# Root cause: npm omit=dev was being set, likely from global npm config or NODE_ENV=production
include=dev
